CaveBridge — how to run on Linux
================================

From this folder:

    ./run.sh           (or:  ./cavebridge )

First launch asks for your LLM connection (endpoint URL, API key, model name);
change it anytime in-game with  /config .  Recommended models are in the README.
